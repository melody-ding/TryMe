#!/bin/bash
set -Eeuxo pipefail

js_path="$( cd "$( dirname "${BASH_SOURCE[0]}" )/.." && pwd )"

rm -f $js_path/src/Assets.js
rm -f $js_path/src/ElvCrypto.wasm
rm -f $js_path/src/ElvCrypto.wasm.base64
rm -f $js_path/src/ElvCryptoWasm.js
rm -rf $js_path/dist
rm -f $js_path/*.tmp
